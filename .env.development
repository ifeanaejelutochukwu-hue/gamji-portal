# Development environment
# Quick login buttons are shown in dev for easy testing
VITE_SHOW_QUICK_LOGIN=true

# Go backend URL — update this when your local Go server is running
VITE_API_URL=http://localhost:8080
